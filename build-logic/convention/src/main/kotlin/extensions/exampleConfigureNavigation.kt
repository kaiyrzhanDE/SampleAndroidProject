package extensions

/**
 * Example of configuring navigation frameworks (Voyager, Jetpack Navigation, Decompose and etc.)
 *
 * internal fun Project.configureNavigation() {
 *    dependencies {
 *        add(IMPLEMENTATION, project.getLibrary("decompose"))
 *        add(IMPLEMENTATION, project.getLibrary("decompose-extensions"))
 *    }
 *}
 */